version: "3.8"

services:
  kafka:
    image: docker.io/bitnami/kafka:3.8
    container_name: kafka
    volumes:
      - "kafka_data:/bitnami"
    networks:
      - connection-network
    environment:
      - KAFKA_CFG_NODE_ID=0
      - KAFKA_CFG_PROCESS_ROLES=controller,broker
      - KAFKA_CFG_CONTROLLER_QUORUM_VOTERS=0@kafka:9093
      - KAFKA_CFG_LISTENERS=PLAINTEXT://:9092,CONTROLLER://:9093
      - KAFKA_CFG_ADVERTISED_LISTENERS=PLAINTEXT://kafka:9092
      - KAFKA_CFG_LISTENER_SECURITY_PROTOCOL_MAP=CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT
      - KAFKA_CFG_CONTROLLER_LISTENER_NAMES=CONTROLLER
      - KAFKA_CFG_INTER_BROKER_LISTENER_NAME=PLAINTEXT

  kafbat-ui:
    container_name: kafbat-ui
    image: ghcr.io/kafbat/kafka-ui:latest
    ports:
      - 8090:8080
    depends_on:
      - kafka
    networks:
      - connection-network
    environment:
      KAFKA_CLUSTERS_0_NAME: local
      KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS: kafka:9092
      KAFKA_CLUSTERS_0_KSQLDBSERVER: http://ksqldb0:8088

  notification-db:
    image: postgres
    container_name: notification-db
    environment:
      - POSTGRES_DB=notificationDB
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=password
    networks:
      - connection-network
    volumes:
      - notification-db-data:/var/lib/postgresql/data

  posts-db:
    image: postgres
    container_name: posts-db
    environment:
      - POSTGRES_DB=postsDB
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=password
    networks:
      - connection-network
    volumes:
      - posts-db-data:/var/lib/postgresql/data

  user-db:
    image: postgres
    container_name: user-db
    environment:
      - POSTGRES_DB=userDB
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=password
    networks:
      - connection-network
    volumes:
      - user-db-data:/var/lib/postgresql/data

  connections-db:
    image: neo4j
    container_name: connections-db
    environment:
      - NEO4J_AUTH=neo4j/password  # Set your username and password
    ports:
      - "7474:7474"  # HTTP port
      - "7687:7687"  # Bolt port
    volumes:
      - connections-db-data:/data  # Persist data
    networks:
      - connection-network

  discovery-server:
    image: anshulmalokar/connection-app-discovery-service:v1
    container_name: discovery-server
    networks:
      - connection-network
    ports:
      - "8761:8761"

  posts-service:
    image: anshulmalokar/connection-app-posts-service:v1
    container_name: posts-service
    networks:
      - connection-network
    depends_on:
      - discovery-server
      - posts-db
      - kafka

  user-service:
    image: anshulmalokar/connection-app-user-service:v1
    container_name: user-service
    networks:
      - connection-network
    depends_on:
      - discovery-server
      - user-db
      - kafka

  notification-service:
    image: anshulmalokar/connection-app-notification-service:v1
    container_name: notification-service
    networks:
      - connection-network
    depends_on:
      - discovery-server
      - notification-db
      - kafka

  connections-service:
    image: anshulmalokar/connection-app-connection-service:v1
    container_name: connections-service
    networks:
      - connection-network
    depends_on:
      - discovery-server
      - connections-db
      - kafka

  api-gateway:
    image: anshulmalokar/connection-app-api-gateway:v1
    container_name: api-gateway
    ports:
      - "8080:8080"
    networks:
      - connection-network
    depends_on:
      - discovery-server

networks:
  connection-network:

volumes:
  kafka_data:
  notification-db-data:
  posts-db-data:
  user-db-data:
  connections-db-data:
